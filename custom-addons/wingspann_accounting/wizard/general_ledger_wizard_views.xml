<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_general_ledger_wizard_form" model="ir.ui.view">
        <field name="name">wingspann.general.ledger.wizard.form</field>
        <field name="model">wingspann.general.ledger.wizard</field>
        <field name="arch" type="xml">
            <form string="General Ledger Report">
                <group>
                    <group>
                        <field name="date_from"/>
                        <field name="date_to"/>
                    </group>
                    <group>
                        <field name="target_move" widget="radio"/>
                    </group>
                </group>
                <group>
                    <field name="account_ids" widget="many2many_tags" options="{'no_create': True}"/>
                </group>
                <footer>
                    <button name="action_print_report" string="Print PDF" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_general_ledger_wizard" model="ir.actions.act_window">
        <field name="name">General Ledger</field>
        <field name="res_model">wingspann.general.ledger.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <menuitem id="menu_wingspann_accounting_reports" 
              name="Reporting" 
              parent="menu_wingspann_accounting_root" 
              sequence="50"/>

    <menuitem id="menu_wingspann_general_ledger" 
              name="General Ledger" 
              parent="menu_wingspann_accounting_reports" 
              action="action_general_ledger_wizard" 
              sequence="1"/>
</odoo>
